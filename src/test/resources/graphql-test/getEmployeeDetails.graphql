query getEmployeeDetails($id: String!) {
    employeeById(id: $id) {
        name
        role
    }
}

mutation createAddress($addressInput: AddressInput){
    addAddress( address: $addressInput) {
        id
        addressLine1
    }
}